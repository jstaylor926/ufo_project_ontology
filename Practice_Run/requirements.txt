# --------------------------------------------------------------------------
# UFO V2 — Practice_Run local execution requirements
# --------------------------------------------------------------------------
# Install with:
#     pip install -r requirements.txt
#
# Tested on Python 3.10 / 3.11. PySpark 3.5+ requires Java 11 or 17 installed
# and on PATH (set JAVA_HOME if needed). On macOS:
#     brew install openjdk@17
# On Windows download Temurin 17 from https://adoptium.net/.
# --------------------------------------------------------------------------

pyspark==3.5.1          # core Spark engine used by every V2 bronze transform
pandas>=2.0,<3.0        # only used by the runners for quick CSV inspection
pyarrow>=14.0           # parquet writer (Spark uses it under the hood too)
python-dateutil>=2.8    # safe-side timestamp parsing in ad-hoc checks
